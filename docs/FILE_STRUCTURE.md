# Graduation-Support-Map 프로젝트 파일 구조

## 전체 구조 개요

```
Graduation-Support-Map/
├── client/              # 프론트엔드 (React)
├── server/              # 백엔드 (Node.js/Express)
└── docs/                # 프로젝트 문서
```

---

## 📁 Client (프론트엔드)

```
client/
├── public/
│   └── index.html                 # HTML 진입점
│
├── src/
│   ├── assets/                    # 정적 파일 (이미지, 아이콘, 폰트)
│   │
│   ├── components/                # 재사용 가능한 UI 컴포넌트
│   │   ├── Navbar.jsx            # 상단 네비게이션 바
│   │   ├── Footer.jsx            # 하단 푸터
│   │   └── Card.jsx              # 전시회 카드 컴포넌트
│   │
│   ├── data/                      # 데이터 파일
│   │   ├── exhibitions.js        # 전시회 데이터 (JS)
│   │   ├── exhibitions.json      # 전시회 데이터 (JSON)
│   │   └── schools.json          # 학교 데이터
│   │
│   ├── pages/                     # 페이지 컴포넌트
│   │   ├── Home.jsx              # 메인 페이지 (전시회 리스트)
│   │   ├── Login.jsx             # 로그인 페이지
│   │   ├── Signup.jsx            # 회원가입 페이지
│   │   ├── SchoolDetail.jsx      # 학교별 상세 페이지
│   │   └── Calendar.jsx          # 달력 페이지
│   │
│   ├── styles/                    # CSS 스타일 파일
│   │   ├── global.css            # 전역 스타일
│   │   └── calendar.css          # 달력 전용 스타일
│   │
│   ├── App.jsx                    # 라우터 설정 및 앱 루트
│   └── main.jsx                   # React 진입점
|
└── package.json                   # 프론트엔드 의존성
```

### 주요 기능별 파일

| 기능 | 관련 파일 |
|------|-----------|
| 라우팅 | `App.jsx` |
| 앱 초기화 | `main.jsx` |
| 전역 스타일 | `styles/global.css` |
| 네비게이션 | `components/Navbar.jsx` |
| 전시회 표시 | `components/Card.jsx`, `pages/Home.jsx` |
| 학교 상세 정보 | `pages/SchoolDetail.jsx` |
| 달력 기능 | `pages/Calendar.jsx`, `styles/calendar.css` |
| 사용자 인증 | `pages/Login.jsx`, `pages/Signup.jsx` |

---

## 📁 Server (백엔드)

```
server/
├── config/                        # 설정 파일
│   ├── database.js               # 데이터베이스 설정
│   └── session.js                # 세션 설정
│
├── controllers/                   # 비즈니스 로직
│   ├── authController.js         # 인증 관련 로직
│   ├── schoolController.js       # 학교 정보 처리
│   ├── exhibitionController.js   # 전시회 정보 처리
│   └── messageController.js      # 메시지 처리
│
├── models/                        # 데이터 모델
│   ├── userModel.js              # 사용자 모델
│   ├── schoolModel.js            # 학교 모델
│   ├── exhibitionModel.js        # 전시회 모델
│   └── messageModel.js           # 메시지 모델
│
├── routes/                        # API 라우트
│   ├── index.js                  # 라우트 통합
│   ├── authRoutes.js             # 인증 API
│   ├── schoolRoutes.js           # 학교 API
│   ├── exhibitionRoutes.js       # 전시회 API
│   └── messageRoutes.js          # 메시지 API
│
├── middlewares/                   # 미들웨어
│   ├── auth.js                   # 인증 미들웨어
│   ├── validation.js             # 유효성 검사
│   ├── errorHandler.js           # 에러 핸들링
│   └── logger.js                 # 로깅
│
├── utils/                         # 유틸리티 함수
│   ├── response.js               # 응답 포맷팅
│   └── validation.js             # 검증 헬퍼
│
├── uploads/                       # 업로드 파일 저장소
│   ├── posters/                  # 전시회 포스터
│   └── logos/                    # 학교 로고
│
├── app.js                         # Express 앱 설정
├── server.js                      # 서버 실행 파일
└── package.json                   # 백엔드 의존성
```

### API 엔드포인트 구조

| 경로 | 파일 | 설명 |
|------|------|------|
| `/api/auth/*` | `routes/authRoutes.js` | 로그인, 회원가입, 로그아웃 |
| `/api/schools/*` | `routes/schoolRoutes.js` | 학교 정보 CRUD |
| `/api/exhibitions/*` | `routes/exhibitionRoutes.js` | 전시회 정보 CRUD |
| `/api/messages/*` | `routes/messageRoutes.js` | 메시지 기능 |

---

## 📁 Docs (문서)

```
docs/
├── API.md                         # API 문서
├── DEPLOYMENT.md                  # 배포 가이드
├── ERD.png                        # 데이터베이스 ERD
└── FILE_STRUCTURE.md              # 파일 구조 문서 (현재 파일)
```

---

## 🔧 설정 파일

| 파일 | 위치 | 설명 |
|------|------|------|
| `package.json` | `/client/` | 프론트엔드 의존성 및 스크립트 |
| `package.json` | `/server/` | 백엔드 의존성 및 스크립트 |
| `.env` | `/server/` | 환경 변수 (DB, 포트 등) |
| `.gitignore` | 루트 | Git 제외 파일 목록 |

---


### 권장 디렉토리 구조 준수
- 모든 React 소스 코드는 `/client/src/` 하위에 위치
- 정적 파일은 `/client/public/` 또는 `/client/src/assets/` 사용
- 서버 업로드 파일은 `/server/uploads/` 하위에 저장

---

## 📚 추가 문서

- API 명세: [API.md](./API.md)
- 배포 가이드: [DEPLOYMENT.md](./DEPLOYMENT.md)
- 데이터베이스 설계: [ERD.png](./ERD.png)

---

**프로젝트**: 2025 웹스튜디오 텀프로젝트
